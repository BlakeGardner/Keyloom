# README demo recordings and the project website: versioned here, but kept
# out of `git archive`, which scripts/build-source-packages.sh uses to make
# the source tarball and GitHub uses for a tag's source archive (so the
# Debian, RPM, and Arch packages never see them).
docs/media/ export-ignore
site/ export-ignore
